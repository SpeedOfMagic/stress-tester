#include <bits/stdc++.h>
 
using namespace std;

void run() {
    int a; cin >> a;
    cout << a << endl;
}
 
signed main() {
    ios::sync_with_stdio(0);
    cin.tie();
    int t = 1;
    // cin >> t;
    for (int test = 1; test <= t; ++test) {
        run();
    }
}